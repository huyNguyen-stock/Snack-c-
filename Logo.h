#pragma once

void DrawLogoTeam(int maxColumn, int maxRow);	//Vẽ logo Team ở giữa màn hình

void DrawLogoSnake(int x, int y);	//Vẽ logo Snake tại tọa độ (x,y)
int getLSw();	//Trả về chiều rộng của logo Snake (get Logo Snake width)
int getLSh();	//Trả về chiều cao của logo Snake (get Logo Snake height)